# Version 2024-01-11

- The site now uses the Material theme for mkdocs so the site will look better and load faster
- Fixed image paths
- Added a link to the repo within the website
- The primary has now been changed to deep orange
- I've only now just realised that the markdown files that actually make up the site aren't visible in the main branch of the GitHub repo, so I fixed that.